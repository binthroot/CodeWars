function formatDuration(seconds) {
  const year = 31557600;
  const day = 86400;
  const hour = 3600;
  const minute = 60;
  let secondsLeft = seconds;
  let resultString = '';

  if (secondLefts > year) {
    let yearString = Math.floor(secondsLeft / year);
  }
}

// initial work for human readable duration code wars 4kyu
